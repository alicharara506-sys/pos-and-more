# Copy to .env and fill in.
#
# Must be reachable from your physical device or simulator — NOT `localhost`,
# since a device/simulator does not share your development machine's network
# namespace. Use your machine's LAN IP, or a tunneled/deployed API URL.
EXPO_PUBLIC_API_URL=http://192.168.1.100:4000/api/v1
